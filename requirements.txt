
virtuelenv -p python3 env
source env/bin/activate

pip install django
pip install djangorestframework
pip install markdown       
pip install django-filter  
pip install requests
pip install psycopg2



settings.py:
    add 'rest_framework','rest_framework.authtoken' to INSTALLED_APP
    add  REST_FRAMEWORK = {
        'DEFAULT_AUTHENTICATION_CLASSES': [
            'rest_framework.authentication.TokenAuthentication',
        ],
        'DEFAULT_RENDERER_CLASSES': [
            'rest_framework.renderers.JSONRenderer',
            'rest_framework.renderers.BrowsableAPIRenderer',
        ]
    }
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.postgresql_psycopg2',
            'NAME': 'coronawatch',
            'USER': 'django',
            'PASSWORD': 'rafix',
            'HOST': 'localhost',
            'PORT': '',
        }
    }
    
    AUTH_USER_MODEL = 'users.User'


POSTGRES :
    CREATE DATABASE coronawatch;
    CREATE USER django WITH PASSWORD 'rafix';
    ALTER ROLE django SET client_encoding TO 'utf8';
    GRANT ALL PRIVILEGES ON DATABASE coronawatch TO django;



to enable inspectdb:
    psql coronawatch -c "GRANT ALL ON ALL TABLES IN SCHEMA public to django;"
    psql coronawatch -c "GRANT ALL ON ALL SEQUENCES IN SCHEMA public to django;"
    psql coronawatch -c "GRANT ALL ON ALL FUNCTIONS IN SCHEMA public to django;"


Download Postman:
    https://www.postman.com/downloads/
    
Generer documentation de l'api:
    https://www.django-rest-framework.org/api-guide/schemas/
    pip install pyyaml 
    pip install uritemplate


